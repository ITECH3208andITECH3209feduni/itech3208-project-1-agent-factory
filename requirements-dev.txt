# Agent Factory — Test/Dev Dependencies (PROJ-354-363)
# Install with: pip install -r requirements.txt -r requirements_web.txt -r requirements-dev.txt

pytest>=8.0.0             # Test runner
pytest-cov>=5.0.0         # Coverage reporting (coverage badge — PROJ-374-378)
httpx>=0.27.0             # Required by fastapi.testclient.TestClient
pytest-asyncio>=0.24.0    # Required for `async def test_...` in tests/test_amazon_scraper.py;
                           # pytest.ini's asyncio_mode=auto is this package's setting — it was
                           # never actually installed, so it silently no-opped and async tests
                           # failed with "async def functions are not natively supported."
